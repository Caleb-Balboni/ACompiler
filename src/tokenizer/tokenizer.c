#include <stdio.h>
#include "tokenizer.h"
#include "tokens.h"

TokenList tokenize(FILE* sourcefile) {
	printf("sucessfully got to tokenize");
	return (TokenList){0};
}

Token parseString(char* input) {
	printf("got to parse string func");

	return (Token){0};
}	
